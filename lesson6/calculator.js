// loyalty, highValue, supportive, beauty
// all out of 10 each, for total 40 points
// if she scores over 30, she is girlfriend material
// Create a calculator that prompts the user for each of the four values, then adds them together. If they are over 30 points, console log "let's go". If they are 20-30, console log "nah". If they are less than 20, console log "say you're such a great friend".

const loyalty = prompt("loyalty")
console.log(+loyalty + +loyalty)